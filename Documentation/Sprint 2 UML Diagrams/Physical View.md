# Physical View
We have the user’s device (Patient / Staff / Admin), these then access the system through a browser, which runs the CareQueue Web App (HTML/CSS/JS).

That web app communicates with the backend over HTTPS, which is important because it encrypts all data in transit

(../../images/PhysicalView.png) 
